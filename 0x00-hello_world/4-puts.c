#include<stdio.h>

/**
 * main Entry point (Print using puts function)
 *
 * Return: Always 0 (Success), non-zero (Failed)
 */
int main(void)
{
	puts("Programming is like building a multilingual puzzle");
	return (0);
}
