#include "main.h"

/**
 * _abs - Computes absolute value of integer
 * @n: the int to check
 * Return: The absolute value of int n
 */
int _abs(int n)
{
	if (n >= 0)
	{
		return (n);
	}
	else
	{
		return (-n);
	}
}
